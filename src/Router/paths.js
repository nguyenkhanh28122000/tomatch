const authPath = {
    login: '/login',
    register: '/register',
};

const privatePath = {
    user: '/user',
    personalResults: '/personal-results',
    groupResults: '/group-results',
};

export { authPath, privatePath };
